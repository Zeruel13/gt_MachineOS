--load requirements
local utils = require("utils")


local textColor = utils.colors.purple
local outlineColor = utils.colors.yellow
local sectionColor = utils.colors.blue
local titleColor = utils.colors.cyan

local userColors = {

	textColor = textColor,
	outlineColor = outlineColor,
	sectionColor = sectionColor,
	titleColor = titleColor,

}

return userColors